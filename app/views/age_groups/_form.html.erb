<%= form_with(model: age_group, local: true) do |form| %>
  <% if age_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(age_group.errors.count, "error") %> prohibited this age_group from being saved:</h2>

      <ul>
      <% age_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label t('name') %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label t('min_age') %>
    <%= form.number_field :min_age %>
  </div>

  <div class="field">
    <%= form.label t('max_age') %>
    <%= form.number_field :max_age %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
